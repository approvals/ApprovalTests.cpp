#pragma once

// These are in a header rather than in the CMake file,
// to allow quick experimentation without waiting for CMake
// to re-run.

#undef APPROVAL_TESTS_HIDE_DEPRECATED_CODE
#undef APPROVAL_TESTS_SHOW_DEPRECATION_WARNINGS
